# Final-Project-Crypto
A Python-based encryption pipeline that automatically detects new files, encrypts them with AES-256, secures keys using RSA, verifies integrity with digital signatures, and manages all metadata through JSON logging.
